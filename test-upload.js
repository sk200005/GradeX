const fs = require('fs');
const FormData = require('form-data');
const fetch = require('node-fetch'); // Assuming it's not available, I should just run app.js and make an HTTP request or test the function directly.
